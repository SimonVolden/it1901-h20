package treningsapp.ui;

/*

import static org.junit.jupiter.api.Assertions.assertNotNull;

import java.net.URI;
import java.net.URISyntaxException;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
//import org.testfx.framework.junit5.ApplicationTest;

import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.stage.Stage;
*/
import org.testfx.framework.junit5.ApplicationTest;

public class integrationTest extends ApplicationTest{
    /*
    private MainSceneController controller;

    @Override
    public void start(final Stage stage) throws Exception {
    final FXMLLoader loader = new FXMLLoader(getClass().getResource("MainScene.fxml"));
    final Parent root = loader.load();
    this.controller = loader.getController();
    stage.setScene(new Scene(root));
    stage.show();
  }

  @BeforeEach
  public void setupItems() throws URISyntaxException {
    // same as in test-todolist.json (should perhaps read it instead)
    
      String port = System.getProperty("todo.port");
      assertNotNull(port, "No todo.port system property set");
      URI baseUri = new URI("http://localhost:" + port + "/workoutProgram/");
      System.out.println("Base RemoteTodoModelAcces URI: " + baseUri);
      FxApp.setWorkoutModelAccess(new RemoteModelAccess(baseUri));
    }
    @Test
    public void testController_initial() {
    assertNotNull(this.controller);
  }
  */


    
}